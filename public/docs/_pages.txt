? this is where the directory is defined
? comments do NOT-YET exist at the end of lines
? lines and filenames that start with "? " are comments and get ignored
? to clarify, that is a question mark (?) followed by a space ( )

? start a folder name with "?c " to make it show up as collapsed

Credits/
  Everyone
  OGs
Brawl/
  ?c Structs/
    ? soResourceModuleImpl
    ? soModelModuleImpl
    ? soMotionModuleImpl
    soPostureModuleImpl
    ? soGroundModuleImpl
    ? soSituationModuleImpl
    ? soTeamModuleImpl
    ? soCollisionAttackModuleImpl
    ? soCollisionHitModuleImpl
    ? soCollisionShieldModuleImpl
    ? soCollisionShieldModuleImpl
    ? soCollisionShieldModuleNull
    ? soCollisionCatchModuleImpl
    ? soCollisionSearchModuleNull
    ? soDamageModuleActor
    ? soCatchModuleImpl
    ? soCaptureModuleImpl
    ? ftStopModuleImpl
    ? soTurnModuleImpl
    ? soShakeModuleImpl
    ? soSoundModuleImpl
    ? soLinkModuleImpl
    ? soVisibilityModuleImpl
    ? ftControllerModuleImpl
    ? soCameraModuleImpl
    ? soWorkManageModuleImpl
    ? soDebugModuleNull
    ? soAnimCmdModuleImpl
    ? soStatusModuleImpl
    ? ftGeneralTermDisideModuleImpl
    ? ftSwitchDecideModuleImpl
    ? soKineticModuleGenericImpl
    ? soEventManageModuleImpl
    ? soGenerateArticleManageModuleImpl
    ? soEffectModuleImpl
    ? ftComboModuleImpl
    ? soAreaModuleImpl
    ? soTerritoryModuleNull
    ? soTargetSearchModuleNull
    ? soPhysicsModuleImpl
    ? soSlopeModuleImpl
    ? soShadowModuleImpl
    ? soItemManageModuleImpl
    ? soColorBlendModuleImpl
    ? soJostleModuleImpl
    ? ftAbnormalModuleImpl
    ? soSlowModuleImpl
    ? soReflectModuleNull
    ? soHeapModuleImpl
    ? ftParamCustomizeModuleImpl
    ? ftGlowModuleImpl
  Kirby_Hats/
    Subactions
    Variables
  Modules/
    Module_Map_Files
    Module_Table
  Variables/
    IC_Constants
    IC_Memory_Map
    Overview
  AI_Modding/
    introduction
    Basics/
      getting_started
      CPU_level_values
      include_files
      as_files
      AIPD_file
      ATKD_file
    Development_Techniques/
      Fixing_Melee_Airdodge_SDs
    ?c Falconbot/
      Philosophy
  Actions
  Article_Variables
  Articles
  Buttons
  Character_Specific_Variables
  Character_Subactions
  Data_Types_List
  Effect_Pac_ids
  Enemies
  Event_Match_Files
  Events
  Fighter_Module_Constants
  Graphical_Effects
  Hitbox_Flags
  Item_Generation_Table
  Items
Tools/
  TestPage
index
changelog